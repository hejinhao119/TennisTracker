# ADR-005: Evaluate against a single-agent baseline

Multiple agents add latency and orchestration complexity. We therefore require a matched single-agent baseline before claiming that specialization improves grounding or recommendation quality. The baseline is a planned evaluation artifact, not an assumed advantage.